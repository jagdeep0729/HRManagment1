@model IEnumerable<HRManagment1.Models.JobOpeningApplicant>

@{
    ViewData["Title"] = "MyApplications";
}

<h1>MyApplications</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Applicant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobOpening)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Applicant.ApplicantId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JobOpening.JobPositions)
            </td>
           @*  <td>
                <a asp-action="Edit" asp-route-id="@item.JobOpeningApplicantId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobOpeningApplicantId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobOpeningApplicantId">Delete</a>
            </td> *@
        </tr>
}
    </tbody>
</table>
